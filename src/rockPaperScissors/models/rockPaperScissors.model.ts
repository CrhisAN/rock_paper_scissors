export interface GameScore {
  player: number
  cpu: number
}

export interface OptionTool {
  id: number
  name: string
  iconClass: string
}

export interface OptionNames {
  rock: string;
  paper: string;
  scissors: string;
}